# MySQL

Central mysql database server for the home network.

# Running

Copy the env file:

`cp .env.example .env`

Edit the .env file

`nano .env`

Fill out the related fields:

```
MYSQL_USER=
MYSQL_ROOT_PASSWORD=
MYSQL_PASS=
```

Run the container:

`docker-compose up`


